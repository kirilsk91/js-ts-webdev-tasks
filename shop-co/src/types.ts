export type CtaStatPair = {
  num: number;
  text: string;
};

export type FooterLinks = {
  colTitle: string;
  links: string[];
};

export type Category = {
  slug: string;
  name: string;
  url: string;
};
