export type CtaStatPair = {
  num: number;
  text: string;
};

export type FooterLinks = {
  colTitle: string;
  links: string[];
};

export type Categories = {
  slug: string;
  name: string;
  url: string;
};
